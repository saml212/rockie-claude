---
file: DISMISSALS.md
schema_version: 1
last_updated: ${generated_at}
append_only: true
---

# DISMISSALS — dead ends, "never again"

This file is **append-only**. Entries are not edited or removed; new
entries supersede older ones with explicit dated annotations. The
agent reads this file as a *blocker filter* before proposing anything.

If an idea overlaps with a dismissal here, surface the overlap rather
than papering over it. The researcher chose to mark this dead for a
reason.

<!-- APPEND_HERE -->

${dismissals_block}

---

## How to use this file

Before recommending an approach, run a keyword scan against this
file. If overlap found, your message must lead with: *"this overlaps
with a dismissal: '<entry summary>' — proceed anyway?"*. Do not
silently re-propose dead ideas.
